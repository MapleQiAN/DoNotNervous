---
phase: 08-codebase-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test-setup.ts
  - src/components/mascot/ANIMATIONS.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "canvas-confetti is mocked globally so no test file throws unhandled exceptions from celebrate.ts imports"
    - "domain/streaks.ts and its test remain untouched — reserved for v2 STRK-06/STRK-07"
    - "Lottie placeholder status is documented in a dedicated file listing each animation and replacement guidance"
  artifacts:
    - path: "src/test-setup.ts"
      provides: "Global canvas-confetti mock for all tests"
      contains: "vi.mock('canvas-confetti')"
    - path: "src/components/mascot/ANIMATIONS.md"
      provides: "Lottie placeholder documentation"
      min_lines: 20
  key_links:
    - from: "src/test-setup.ts"
      to: "canvas-confetti"
      via: "vi.mock"
      pattern: "vi\\.mock\\('canvas-confetti'\\)"
    - from: "src/lib/celebrate.ts"
      to: "canvas-confetti"
      via: "import"
      pattern: "import confetti from 'canvas-confetti'"
---

<objective>
Resolve three tech-debt items from the v1.0 audit: (1) keep domain/streaks.ts intact for v2 features per D-01, (2) mock canvas-confetti globally in test setup per D-02, and (3) document Lottie placeholder status per D-03.

Purpose: Eliminate test noise from canvas-confetti and create a reference for future animation asset work.

Output: Updated test-setup.ts, new ANIMATIONS.md documentation file. No code deletion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-codebase-cleanup/08-CONTEXT.md

<interfaces>
<!-- celebrate.ts — the file whose canvas-confetti import causes test noise -->
From src/lib/celebrate.ts:
```typescript
import confetti from 'canvas-confetti'
export function celebrate(options: CelebrateOptions): void
export const celebrateTaskComplete: () => void
export const celebrateMoodLog: () => void
export const celebrateRedemption: () => void
export const celebrateStreakMilestone: () => void
```

<!-- test-setup.ts — global setup file where mock must be added -->
Current contents of src/test-setup.ts:
```typescript
import '@testing-library/jest-dom/vitest'
import 'fake-indexeddb/auto'
```

<!-- Animation files in public/animations/ -->
- cat-celebrate.json (2065 bytes)
- cat-encourage.json (2511 bytes)
- cat-idle.json (1942 bytes)
- cat-sleepy.json (2763 bytes)

<!-- Mascot animation states from mascotStore.ts -->
type MascotAnimation = 'idle' | 'celebrate' | 'encourage' | 'sleepy'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add global canvas-confetti mock to test setup (D-02)</name>
  <files>src/test-setup.ts</files>
  <read_first>
  - src/test-setup.ts (current contents — 2 lines: jest-dom + fake-indexeddb imports)
  - src/lib/celebrate.ts (understand how canvas-confetti is consumed — default import, called as function)
  - vitest.config.ts (confirm test-setup.ts is in setupFiles array)
  </read_first>
  <action>
Per D-02: Add a `vi.mock('canvas-confetti')` call to `src/test-setup.ts`.

The mock must return a no-op function matching the `canvas-confetti` default export signature. Add the following AFTER the existing two import lines in `src/test-setup.ts`:

```typescript
import { vi } from 'vitest'

vi.mock('canvas-confetti', () => ({
  default: vi.fn(),
}))
```

This mock:
- Uses `vi.mock` (hoisted by Vitest to run before all imports) so it covers every test file that transitively imports `celebrate.ts`
- Returns an object with `default` as a `vi.fn()` — matching the `import confetti from 'canvas-confetti'` default import in `celebrate.ts`
- Needs no reset/cleanup because Vitest auto-resets mocks between test files

The final file should contain exactly:
```typescript
import '@testing-library/jest-dom/vitest'
import 'fake-indexeddb/auto'

import { vi } from 'vitest'

vi.mock('canvas-confetti', () => ({
  default: vi.fn(),
}))
```
  </action>
  <verify>
    <automated>cd "E:/DoNotNervous" && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass with zero canvas-confetti-related errors or warnings. `src/test-setup.ts` contains `vi.mock('canvas-confetti')`.</done>
  <acceptance_criteria>
  - `src/test-setup.ts` contains the exact string `vi.mock('canvas-confetti'`
  - `src/test-setup.ts` contains `import { vi } from 'vitest'`
  - `rtk vitest run` exits with code 0 and shows no unhandled exception from canvas-confetti
  - File has exactly 3 import lines and 1 vi.mock call (7 lines total including blank line separator)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create Lottie placeholder documentation (D-03)</name>
  <files>src/components/mascot/ANIMATIONS.md</files>
  <read_first>
  - src/components/mascot/Mascot.tsx (see which animation files are loaded and how states map to files)
  - src/stores/mascotStore.ts (see the MascotAnimation type and animation state machine)
  - public/animations/cat-idle.json (read first 20 lines to understand placeholder structure — small shape layers with basic transforms, no real cat artwork)
  </read_first>
  <action>
Per D-03: Create `src/components/mascot/ANIMATIONS.md` documenting the placeholder status of all Lottie animation assets.

The file must contain:

1. A header: `# Mascot Animations — Asset Status`
2. A table with columns: Animation | File | State | Trigger | Replacement Notes
3. One row per animation:
   - `idle` | `public/animations/cat-idle.json` | Placeholder | Default, breathing loop | Gentle sway/breathe, cat sitting relaxed
   - `celebrate` | `public/animations/cat-celebrate.json` | Placeholder | Task complete, reward redeem | Jumping/bouncing with raised paws, confetti-friendly pose
   - `encourage` | `public/animations/cat-encourage.json` | Placeholder | Streak milestone (7/14/30 days) | Waving paws, cheering motion
   - `sleepy` | `public/animations/cat-sleepy.json` | Placeholder | No current trigger (future use) | Drowsy eyes, slow breathing, maybe Z particles
4. A `## Technical Details` section documenting:
   - Renderer: lottie-react (After Effects JSON format)
   - File sizes: ~2-3KB each (placeholder shapes only — production assets will be larger)
   - All animations loop via Lottie `loop={true}` prop
   - Animation speed controlled via mascotStore, default 1x
5. A `## Replacement Checklist` section with guidance:
   - Export from After Effects via Bodymovin plugin at 60fps, 300x300px comp
   - Keep file size under 100KB per animation
   - Maintain the same JSON filename to avoid import path changes
   - Test in happy-dom environment (Lottie renders to canvas/SVG — verify mock coverage)
6. A note: "domain/streaks.ts is intentionally kept (per D-01) — reserved for v2 STRK-06 earn-back recovery and STRK-07 calendar view features."

Use Markdown format. Keep the file concise (25-40 lines).
  </action>
  <verify>
    <automated>test -f "E:/DoNotNervous/src/components/mascot/ANIMATIONS.md" && grep -c "cat-idle.json" "E:/DoNotNervous/src/components/mascot/ANIMATIONS.md"</automated>
  </verify>
  <done>ANIMATIONS.md exists with a table listing all 4 placeholder animations, replacement guidance, and a reference to D-01 streaks retention.</done>
  <acceptance_criteria>
  - File `src/components/mascot/ANIMATIONS.md` exists
  - File contains the string `cat-idle.json`
  - File contains the string `cat-celebrate.json`
  - File contains the string `cat-encourage.json`
  - File contains the string `cat-sleepy.json`
  - File contains the string `Placeholder` (marking current asset status)
  - File contains the string `Bodymovin` (replacement guidance)
  - File contains the string `STRK-06` (D-01 streaks reference)
  - File is between 25 and 40 lines long
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Run `rtk vitest run` — all tests pass, zero canvas-confetti errors
2. `grep -c "vi.mock('canvas-confetti'" src/test-setup.ts` returns 1
3. `test -f src/components/mascot/ANIMATIONS.md` exits 0
4. `grep "Placeholder" src/components/mascot/ANIMATIONS.md` shows all 4 animation rows
5. `grep "STRK-06" src/components/mascot/ANIMATIONS.md` confirms D-01 reference
6. Verify `src/domain/streaks.ts` was NOT modified: `rtk git diff src/domain/streaks.ts` shows no changes
</verification>

<success_criteria>
- canvas-confetti mocked globally in test-setup.ts — all tests pass cleanly
- Lottie placeholder status documented in src/components/mascot/ANIMATIONS.md
- domain/streaks.ts left untouched (D-01 decision honored — kept for v2)
- Zero files deleted, zero production code behavior changed
</success_criteria>

<output>
After completion, create `.planning/phases/08-codebase-cleanup/08-01-SUMMARY.md`
</output>
