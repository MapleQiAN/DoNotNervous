---
phase: 07-fix-mood-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/mood/MoodCalendar.tsx
autonomous: true
requirements:
  - MOOD-04

must_haves:
  truths:
    - "MoodCalendar uses canonical MOOD_SCORE from domain/summary.ts, not a local divergent copy"
    - "No hardcoded fallback trend data is displayed to users"
    - "No hardcoded date labels appear in the SVG chart"
    - "No hardcoded stats, records, or insights are shown when real data is absent"
    - "MoodCalendar and summary charts produce identical scores for the same emoji"
  artifacts:
    - path: "src/components/mood/MoodCalendar.tsx"
      provides: "MoodCalendar with canonical mood scores and real data display"
      contains: "import.*MOOD_SCORE.*from.*domain/summary"
  key_links:
    - from: "src/components/mood/MoodCalendar.tsx"
      to: "src/domain/summary.ts"
      via: "import { MOOD_SCORE }"
      pattern: "import.*\\{.*MOOD_SCORE.*\\}.*from.*['\"].*domain/summary"
---

<objective>
Fix MoodCalendar divergent mood score mapping and remove all hardcoded fallback/demo data.

Purpose: MoodCalendar defines a local `moodScore` mapping that disagrees with the canonical `MOOD_SCORE` from domain/summary.ts (3 emojis have wrong scores: happy=4 not 5, calm=3 not 4, neutral=2 not 3). Additionally, the component shows hardcoded fake trend data, hardcoded date labels, hardcoded stats, hardcoded records, and hardcoded insights to users when no real data exists. This gap closure consolidates to the canonical source and replaces all fake data with proper empty states.

Output: Single modified file `src/components/mood/MoodCalendar.tsx` with all fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/domain/summary.ts
@src/domain/types.ts
@src/domain/mood.ts
@src/components/mood/MoodCalendar.tsx

<interfaces>
<!-- Canonical mood score mapping — the single source of truth -->

From src/domain/summary.ts (lines 9-18):
```typescript
export const MOOD_SCORE: Record<MoodEmoji, number> = {
  '😊': 5,
  '🥳': 5,
  '😌': 4,
  '💪': 4,
  '😐': 3,
  '😔': 2,
  '😰': 1,
  '😡': 1,
}
```

From src/domain/types.ts:
```typescript
export type MoodEmoji = '😊' | '😌' | '😐' | '😔' | '😰' | '😡' | '🥳' | '💪'
```

From src/components/mood/MoodCalendar.tsx (the LOCAL divergent copy to remove — lines 16-25):
```typescript
const moodScore: Record<MoodEmoji, number> = {
  '😊': 4,   // WRONG — should be 5
  '😌': 3,   // WRONG — should be 4
  '😐': 2,   // WRONG — should be 3
  '😔': 2,
  '😰': 1,
  '😡': 1,
  '🥳': 5,
  '💪': 4,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace divergent moodScore with canonical import and fix trend computation</name>
  <files>src/components/mood/MoodCalendar.tsx</files>
  <read_first>
    - src/components/mood/MoodCalendar.tsx (the file being modified)
    - src/domain/summary.ts (source of canonical MOOD_SCORE, lines 9-18)
  </read_first>
  <action>
1. **Add import for MOOD_SCORE** at the top of the file (after existing imports, around line 7-8):
   ```typescript
   import { MOOD_SCORE } from '../../domain/summary'
   ```

2. **Delete the local `moodScore` constant** (lines 16-25, the entire `const moodScore: Record<MoodEmoji, number> = { ... }` block). This is the divergent copy with 3 wrong scores.

3. **Delete the `fallbackTrend` constant** (line 27):
   ```typescript
   const fallbackTrend = [4, 3.3, 2, 3, 3.8, 4.6, 3]
   ```

4. **Fix the `trend` useMemo** (lines 36-42). Replace with:
   ```typescript
   const trend = useMemo(() => {
     if (allMoods.length === 0) return []
     const recent = allMoods.slice(-7)
     return recent.map((entry) => MOOD_SCORE[entry.emoji])
   }, [allMoods])
   ```
   Changes: (a) returns empty array instead of fallbackTrend when no moods exist, (b) uses `MOOD_SCORE` instead of local `moodScore`, (c) removes the padding loop that mixed real data with fake data.

5. **Fix the SVG chart rendering** (lines 87-100). Replace the entire `<svg>` block with one that handles empty trend:
   - If `trend.length === 0`, render an empty-state message inside the chart area (e.g., a `<text>` centered in the SVG saying "暂无心情数据" with a subtle style).
   - If `trend.length > 0`, render the existing path/dots/lines but:
     - Replace the hardcoded date label `{`5/${12 + index}`}` with actual dates derived from the mood entries. Compute date labels from `allMoods.slice(-7)` — use `format(entry.createdAt, 'M/d')` for each entry. If `trend.length < 7`, only render that many data points and date labels (do NOT pad with fake data).
     - The `path`, `circle`, and emoji text calculations should scale to `trend.length` not assume exactly 7 points. Use `trend.length` for x-spacing: `const step = trend.length > 1 ? 696 / (trend.length - 1) : 0` and compute x as `40 + index * step`.

6. **Fix the stats strip** (lines 103-108). Replace hardcoded values with computed real data:
   ```typescript
   const recentWeekMoods = allMoods.filter(m => {
     const weekAgo = new Date()
     weekAgo.setDate(weekAgo.getDate() - 7)
     return m.createdAt >= weekAgo
   })
   const avgMoodScore = recentWeekMoods.length > 0
     ? (recentWeekMoods.reduce((sum, m) => sum + MOOD_SCORE[m.emoji], 0) / recentWeekMoods.length).toFixed(1)
     : '--'
   ```
   Replace the 4 `<Stat>` components with real or empty-state values:
   - "本周完成任务数" → value="--" with note="暂无数据" (task data not accessible from this component; show empty state rather than fake data)
   - "累计奖励金额" → value="--" with note="暂无数据" (same reasoning)
   - "平均心情指数" → value={recentWeekMoods.length > 0 ? `${avgMoodScore} / 5` : '--'} with note={recentWeekMoods.length > 0 ? `共 ${recentWeekMoods.length} 条记录` : '暂无数据'}
   - "专注时段" → value="--" with note="暂无数据" (not derivable from mood entries)

   NOTE: The first two stats (task count, reward amount) and last stat (focus period) require data from other domain modules not available in this component. Showing '--' / '暂无数据' is correct — fake hardcoded values are worse than no data. The mood average IS derivable here and should show real data.

7. **Fix the records section** (lines 110-128). Replace the fallback logic:
   - Remove the ternary `allMoods.length > 0 ? allMoods.slice(-4).reverse() : fallbackRecords`
   - If `allMoods.length === 0`, render an empty-state message: "暂无心情记录，点击右侧记录今日心情" instead of the record list.
   - If `allMoods.length > 0`, render `allMoods.slice(-4).reverse().map(...)` using the real entry fields only (remove the `isFallback` branching logic entirely since fallbackRecords is gone).

8. **Delete the `fallbackRecords` constant** (lines 207-212) — the entire array.

9. **Fix the insights section** (lines 165-171). Replace hardcoded insight text with a single empty-state insight when no data exists:
   - If `allMoods.length === 0`, render one `<Insight>` with title="记录心情后，这里会出现个性化洞察" text="持续记录，发现你的情绪规律"
   - If `allMoods.length > 0`, render the existing 3 `<Insight>` components but compute the text from real data where possible:
     - Insight 1: "本周共记录 ${recentWeekMoods.length} 次心情" with actual average mood text if enough data.
     - Insights 2 and 3: Replace with simpler real-data insights or a single "继续记录，解锁更多洞察" placeholder. The current hardcoded statistics ("运动3天", "44%", "4.2 vs 3.2") are fabricated and misleading.
  </action>
  <acceptance_criteria>
    - `grep -c "const moodScore" src/components/mood/MoodCalendar.tsx` returns 0 (local constant removed)
    - `grep -c "fallbackTrend" src/components/mood/MoodCalendar.tsx` returns 0 (hardcoded trend removed)
    - `grep -c "fallbackRecords" src/components/mood/MoodCalendar.tsx` returns 0 (hardcoded records removed)
    - `grep -c "5/\${12" src/components/mood/MoodCalendar.tsx` returns 0 (hardcoded dates removed)
    - `grep "import.*MOOD_SCORE.*from.*domain/summary" src/components/mood/MoodCalendar.tsx` returns a match (canonical import present)
    - `grep -c "MOOD_SCORE" src/components/mood/MoodCalendar.tsx` returns at least 2 (import line + usage in trend/stats computation)
    - `grep -c "18 个" src/components/mood/MoodCalendar.tsx` returns 0 (hardcoded stat value removed)
    - `grep -c "¥ 235" src/components/mood/MoodCalendar.tsx` returns 0 (hardcoded stat value removed)
    - `grep -c "3.6 / 5" src/components/mood/MoodCalendar.tsx` returns 0 (hardcoded stat value removed)
    - `grep -c "下午" src/components/mood/MoodCalendar.tsx` returns 0 (hardcoded stat value removed)
    - `grep -c "运动 3 天" src/components/mood/MoodCalendar.tsx` returns 0 (hardcoded insight removed)
    - `grep -c "44%" src/components/mood/MoodCalendar.tsx` returns 0 (hardcoded insight removed)
    - TypeScript compilation succeeds: `rtk npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd E:/DoNotNervous && rtk npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Local `moodScore` constant deleted, replaced by import of canonical `MOOD_SCORE` from `domain/summary.ts`
    - `fallbackTrend` constant deleted, trend useMemo returns empty array when no data
    - `fallbackRecords` constant deleted, records section shows empty state when no data
    - Hardcoded date labels `{`5/${12 + index}`}` replaced with `format(entry.createdAt, 'M/d')`
    - All 4 hardcoded stat values replaced (mood average computed from real data, others show '--')
    - All 3 hardcoded insights replaced with data-driven or empty-state content
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component → Domain | MoodCalendar reads MOOD_SCORE from domain/summary.ts (trusted internal source) |
| Component → IndexedDB | useMoodEntries hook reads user mood entries from Dexie (trusted local data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | MoodCalendar trend display | accept | No external data sources; mood entries are locally authored by the user |
| T-07-02 | Information disclosure | Empty state messages | accept | Empty state text reveals no user data, only generic guidance |
</threat_model>

<verification>
1. TypeScript compilation passes: `rtk npx tsc --noEmit`
2. No local moodScore constant: `grep -c "const moodScore" src/components/mood/MoodCalendar.tsx` == 0
3. No fallbackTrend: `grep -c "fallbackTrend" src/components/mood/MoodCalendar.tsx` == 0
4. No fallbackRecords: `grep -c "fallbackRecords" src/components/mood/MoodCalendar.tsx` == 0
5. No hardcoded dates: `grep -c "5/12" src/components/mood/MoodCalendar.tsx` == 0
6. Canonical import present: `grep "import.*MOOD_SCORE.*from" src/components/mood/MoodCalendar.tsx` matches
7. No hardcoded stats: `grep -c "18 个\|¥ 235\|3.6 / 5" src/components/mood/MoodCalendar.tsx` == 0
</verification>

<success_criteria>
- MoodCalendar imports MOOD_SCORE from domain/summary.ts and uses it for all score lookups
- No divergent local mood score mapping exists in MoodCalendar.tsx
- No hardcoded fallback trend data (fallbackTrend removed, empty state shown instead)
- No hardcoded fallback records (fallbackRecords removed, empty state shown instead)
- SVG chart uses actual dates from mood entry timestamps, not hardcoded `5/12+index`
- Stats strip shows computed mood average or empty state, no fabricated values
- Insights section shows data-driven content or empty state, no fabricated statistics
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-fix-mood-consistency/07-01-SUMMARY.md`
</output>
