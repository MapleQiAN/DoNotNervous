# iOS resources

Place future companion artwork, app icons, and Figma-exported assets here before moving them into an Xcode asset catalog.

The v1 scaffold uses SwiftUI shapes for the original rounded companion so the app can run before final illustration assets are ready.
